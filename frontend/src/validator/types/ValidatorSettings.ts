export type ValidatorSettings = {
    duration: number;
    encrypted: boolean;
    manifest: string;
    media: boolean;
    prefix: string;
    pretty: boolean;
    save: boolean;
    title: string;
    verbose: boolean;
};